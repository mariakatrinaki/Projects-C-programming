#include <stdio.h>

int main(void) {
  printf("Hello World\n");

int number_a = 10;
float number_b = 2.5;
int number_c = 40;
 int multiplication=number_a *number_b;
 float apotelesma = (float)multiplication/number_c;





printf("FOR VALUE %d AND VALUE %.1f the %d is (%d * %.1f)AND DIVIDE THIS WITH VALUE %d the %.3f will be  (%d * %d)/%d",number_a,number_b,multiplication,number_a,number_b,
number_c,apotelesma,number_a,multiplication,number_c
 );
  

  
  return 0;
}
